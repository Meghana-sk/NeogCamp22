// Q. Calculate Kinetic enrgy of a body of mass 'm' and volume 'v'
//K.E = 0.5 * m * v * v

const calcKineticEnergy = (mass, volume) => 0.5 * mass * volume * volume

console.log(calcKineticEnergy(20, 5))